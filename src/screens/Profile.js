import React from "react";
import { MenuContainer, AddMessage } from "../components";

export const ProfileScreen = () => (
  <>
    <MenuContainer />
    <h2>Profile</h2>
    <AddMessage />
  </>
);
