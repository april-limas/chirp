import React from "react";
import { MenuContainer, MessageList } from "../components";

export const ProfileScreen = () => (
  <>
    <MenuContainer />
    <h2>Profile</h2>
    <MessageList />
  </>
);
